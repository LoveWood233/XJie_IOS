## 2026-07-18 至 2026-07-22 — XAGE 集成、画像与体重功能、交付同步

> 更新时间：2026-07-22（Asia/Shanghai）<br>
> 统计范围：本次会话涉及的已提交产品改动，以及这些改动进入 `origin/main`、`upstream/main` 的交付记录。只把 Git 提交、PR 和实际检查支持的内容记为完成。<br>
> 最终交付：来源项目 PR [LoveWood233/XJie_IOS#4](https://github.com/LoveWood233/XJie_IOS/pull/4) 已合入 `origin/main@174fb551`；上游同步 PR [doyoulikelin-wq/XJie_IOS#16](https://github.com/doyoulikelin-wq/XJie_IOS/pull/16) 已合入 `upstream/main@4659dfad`。

### 2026-07-20 — 合并上游主线到 XAGE

提交：`8e6f920` `Merge remote-tracking branch 'upstream/main' into XAGE`

- 将 `upstream/main@802e02a` 合并到当时的 `XAGE@c78ad3a`，集中接入上游近期的 iOS、后端、健康可信链路、膳食记录、用药、报告处理、生产部署与质量合同变更。
- 合并差异规模较大：271 个文件，约 `101,520` 行新增、`16,420` 行删除。主要包括健康画像与报告可信模型、膳食记录服务、用药可信链路、数据库迁移 `0022`–`0025`、生产部署守卫、XAGE 七文件职责拆分以及配套测试/质量清单。
- 该提交属于上游代码集成，不应把其中所有功能都归因于本轮本地开发；需要定位具体功能来源时，应继续查看第二父提交 `802e02a` 之前的上游历史。

### 2026-07-20 — 质量门禁分层与工程说明补充

提交：`437355f` `降低门禁，简化流程。增加注释，提高代码可读性`

- 将回归流程整理为默认轻量检查与可显式启用的严格检查，联动修改 `run_regression_gate.py`、`regression_guard.py`、Python 测试门禁、XCResult 校验器、Release bundle 校验器、CI 和 TestFlight 发布脚本。
- 更新 `REGRESSION_POLICY.md` 与仓库规则，明确日常开发、受影响范围检查、CI、Internal TestFlight 和严格发布检查的职责边界。
- 补充/调整门禁与发布策略测试，覆盖轻量流程、发布策略和回归编排器。
- 新增 `DIETARY_BACKEND_DOCKER_DEPLOYMENT.md`，记录膳食后端 Docker 部署操作；新增 `design-qa.md` 作为视觉检查记录入口。
- 调整 Xcode 工程文件、Medication 模型和 UI 测试基类，并同步更新变更影响记录。

影响规模：19 个文件，`1,279` 行新增、`422` 行删除。

### 2026-07-21 — 首页快捷功能、账号安全与膳食页面优化

提交：`c37f7e4` `增加注释，提高代码可读性。1.首页-快捷功能部分优化，去除管理按钮，改为长按调整顺序。2.用药管理-编辑页面增加快捷添加功能。3.更多-页面布局调整`

- 首页快捷功能改为长按拖动排序，基于稳定 action ID 即时换位并持久化用户顺序；按钮轻点仍进入原业务功能。
- 更多菜单新增“账号与安全”集中入口，展示脱敏手机号并收拢修改密码、退出登录和不可逆账号注销操作。
- 支持与合规页面把相关入口调整为“权限申请与使用情况说明”，并优化备案信息与页面文案。
- 膳食记录区分首次加载失败和用户操作失败；生产服务缺少 dietary-records 路由并返回 404 时，显示明确的服务暂不可用提示，而不是伪装为空数据。
- Meals 页面增加 Preview 隔离入口和大量业务注释，避免 Canvas 自动访问真实账号/服务；同步补充 XAGE 更多菜单 Preview。
- 对 XAGE 首页、组件、数据合同和主页面增加职责说明，降低大型 SwiftUI 页面后续维护成本。

影响规模：8 个文件，`697` 行新增、`214` 行删除。

### 2026-07-21 — 暂停 Git Hooks、画像页面重构与评分展示调整

提交：`a4c6f50` `暂时去除githooks，去除测试门禁`

- 将 `.githooks/pre-commit`、`.githooks/pre-push` 重命名为 `.backup`，暂时停止正常 hook 路径；同时大幅调整 CI 与仓库门禁说明。
- 健康画像页面重新组织为“画像概览 + 统计卡 + 模块列表”，包含基础资料、长期健康标签、安全信息、长期用药和健康目标与计划；前三类支持表单编辑，后两类跳转到唯一业务数据源。
- 调整首页三项健康评分的可展示条件与代理信号说明，页面更多依据 `isReady`、置信度和代理状态呈现结果。
- 简化通用手动体重录入项，固定千克语义并收敛日期/单位输入展示。
- 加强膳食、评分和高强度 UI 流程的回归断言，并同步更新变更影响记录。

影响规模：12 个文件，`949` 行新增、`525` 行删除。

### 2026-07-21 — 健康画像加载容错与编辑体验完善

提交：`ef6db22`（健康画像、体重管理与快捷功能综合改动）

- 健康画像不再因账号画像请求被拦截或读取失败而整页停留在“无法读取”；页面骨架和可调试布局仍可进入，失败状态保留提示与重试入口。
- 画像主页继续使用基础资料、长期健康标签、安全信息、长期用药、健康目标与计划五个核心模块；长期用药和健康计划沿用各自业务模块作为唯一数据源。
- 长期健康标签编辑页移除无实际必要的“填写内容”控件和“关联管理计划”模块，将“家族史”改为“家族病史”。
- 慢病、家族病史、长期异常指标、长期风险因素和主动关注问题等空输入框增加用途说明与常见示例，允许用户不填写非必填项。
- 基础资料保存继续通过健康画像资料链路同步；页面在服务端读取失败时仍保持可编辑入口，便于独立调试 UI。
- 补充画像读取/更新相关 API 单元测试与高强度 UI 回归断言，并同步更新 XCTest 清单和变更影响记录。

### 2026-07-21 — 体重详情页重构、身高录入与本地 BMI

提交：`ef6db22`

- 首页“快捷功能—体重”由弹窗式详情改为正常页面跳转，详情页的“记录体重”使用页内 sheet；取消或保存后回到体重详情页，不再关闭详情页。
- 记录体重 sheet 使用公斤转轮，支持一位小数，避免再进入多余的中间记录页面。
- 最新记录卡展示最近一次体重、记录日期与 BMI；没有身高时显示“还没有记录身高，无法计算 BMI”并提供记录入口，有身高后隐藏入口。
- 新增记录身高 sheet：数字键盘手动输入整数厘米，范围限定为 `50...210 cm`；越界显示“数据范围异常，请填写正确数字。”
- 身高保存后，如果最新记录存在体重，则按 `体重(kg) / 身高(m)²` 在本地计算并刷新 BMI；没有体重时继续显示 `--`。
- 身高数字键盘改为全宽布局，数字按钮与保存按钮高度统一，修复保存按钮异常拉高、数字区未占满屏幕的问题。
- 体重趋势图以当前日期向前取三个月，屏幕宽度约显示半个月并支持横向滚动；纵轴无记录时默认 `45...75 kg`，有记录时按近三个月最小值减 `5 kg`、最大值加 `5 kg` 动态计算。
- 趋势图支持长按选点，放大展示对应日期和体重；相关逻辑集中在 `XAgeMetricTrendView.swift`，并增加 API 与 UI 回归覆盖。

### 2026-07-21 — 更多菜单与快捷功能整理

- 更多菜单的账号能力集中到“账号与安全”：手机号脱敏展示、修改密码、注销账号和退出登录统一收口；退出登录不再单独占据更多菜单顶层。
- “关于”区域提供隐私政策及“权限申请与使用情况说明”入口；意见反馈的“问题或建议”输入框减小圆角。
- 设置页补充 SwiftUI Preview；共享卡片样式区分胶囊背景与可配置圆角卡片，避免用 `Capsule` 承担需要自定义圆角的边框。
- 首页快捷功能保留长按排序，并暂时隐藏尚未准备完成的入口；用药记录在服务端可信状态未完成时继续以失败/读取状态呈现，不伪造本地成功数据。

### 2026-07-21 — 设计 QA 与文档状态更新

提交：`23be54e` `docs: update weight entry design QA status`

- 更新 `design-qa.md` 中体重入口、详情页、记录 sheet、身高录入和按钮比例的验收状态。
- 文档只记录已完成和已验证项目，删除与当前实现不一致的旧描述。

### 2026-07-21 — 合入 origin/main 并同步 upstream/main

- 在交付分支 `codex/merge-local-xage` 上，以非强制方式合并最新 `origin/main` 和 `XAGE`，解决 `quality/change_impact.json` 的唯一内容冲突；合并提交为 `f252007`。
- 本地集成树通过 generic iOS Simulator Debug 构建和 `fast` 轻量回归门禁；该结果不等同于 strict、真机或发布验收。
- PR [#4](https://github.com/LoveWood233/XJie_IOS/pull/4) 合入来源仓库，生成 `origin/main@174fb551`；已确认 `ef6db22`、`23be54e` 和交付合并提交均在其历史中。
- `upstream/main` 受保护，直接推送被拒绝后未使用 force 或绕过保护；改由 `codex/sync-origin-main` 和 PR [#16](https://github.com/doyoulikelin-wq/XJie_IOS/pull/16) 正常交付。
- upstream PR 的 Backend、回归合同、iOS Unit/UI/Release 和汇总 `quality-gate` 均通过后完成合并，最终为 `upstream/main@4659dfad`；已确认其包含 `origin/main@174fb551`。

### 查阅提示与当前风险

- 本节功能已进入 `origin/main@174fb551` 和 `upstream/main@4659dfad`；本地 `XAGE` 分支没有快进到上游合并提交，因此显示落后不代表远端缺少代码。
- 最终集成树已恢复正常 `.githooks/pre-commit` 与 `.githooks/pre-push` 路径；日常默认仍是轻量门禁，不能把 `fast` 或普通 CI 描述为 strict 发布证据。
- `8e6f920` 是大规模上游合并，后续排查回归时应先区分“上游引入”与“合并后本地修改”，再使用父提交做精确 diff。
- 当前交付只证明代码合入及上述构建/CI 结果，不证明 TestFlight build `18` 已完成五项真机签核，也不代表这些新功能已经进入新的 TestFlight 包或生产环境。
- 用药、健康画像和体重数据仍依赖各自服务端接口及可信状态；页面容错保证 UI 可进入，不等于接口数据已经可用。