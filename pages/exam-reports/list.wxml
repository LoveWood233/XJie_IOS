<view class="container">
  <!-- 上传按钮 -->
  <view class="upload-btn" bindtap="uploadExam">
    <text class="upload-icon">📷</text>
    <text class="upload-label">上传体检报告</text>
  </view>

  <view class="loading-box" wx:if="{{loading}}">
    <text class="loading-text">加载中...</text>
  </view>

  <block wx:else>
    <view class="list" wx:if="{{items.length > 0}}">
      <view class="list-item" wx:for="{{items}}" wx:key="id">
        <view class="item-main" data-id="{{item.id}}" bindtap="viewDetail">
          <text class="item-name">{{item.name}}</text>
          <view class="item-meta">
            <text class="item-type tag">{{item.source_type === 'photo' ? '拍照' : item.source_type === 'csv' ? 'CSV' : 'PDF'}}</text>
            <text class="item-status tag {{item.extraction_status === 'done' ? 'tag-success' : 'tag-warning'}}">
              {{item.extraction_status === 'done' ? '已提取' : '处理中'}}
            </text>
            <text class="abnormal-tag tag tag-danger" wx:if="{{item.abnormal_flags && item.abnormal_flags.length > 0}}">
              {{item.abnormal_flags.length}} 项异常
            </text>
            <text class="item-date">{{item.doc_date || ''}}</text>
          </view>
        </view>
        <view class="item-actions">
          <text class="del-btn" data-id="{{item.id}}" catchtap="deleteExam">删除</text>
        </view>
      </view>
    </view>

    <view class="empty-state" wx:else>
      <text class="empty-icon">🔬</text>
      <text class="empty-text">暂无体检报告</text>
      <text class="empty-sub">点击上方按钮上传体检报告</text>
    </view>
  </block>
</view>
